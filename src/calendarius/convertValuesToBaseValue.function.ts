import { TStoredDefinitionUnitMap } from './types';

/**
 * converts a value map into a base system.
 * @param values
 * @param definedSystem
 */
export default function convertValuesToBaseValue(
	values: Map<string, number>,
	definedSystem: TStoredDefinitionUnitMap,
): number {
	return 0;
}
